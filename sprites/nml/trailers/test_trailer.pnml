#define THIS_VEH_ID               veh_trailer_test
// total capacity to be split over tractor / 1st trailer. Some fraction of capacity required for purposes of TE.
#define THIS_CAPACITY             20

#include "../templates/refits/refits_default.tnml"

#define THIS_LENGTH SHORTEN_TO_8_8  // length as fraction of 8; articulated trucks may be shorter than sprites so trailer overlaps  
spriteset(realsprites_veh_trailer_test, "sprites/graphics/trailer_test.png") {
  spriteset_template_truck_28(0, 20) // (search 'sprites/nml' to find where spriteset templates are defined)  
}

#define THIS_SG_ID   sg_veh_trailer_test // ID will also be used for templated item block
spritegroup sg_veh_trailer_test {
    loaded:  [realsprites_veh_trailer_test];
    loading: [realsprites_veh_trailer_test];
}

#include "../templates/template_trailer_item.tnml"

#include "../templates/template_clear_defines.tnml"
